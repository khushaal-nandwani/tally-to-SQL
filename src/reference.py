""" Creates a reference table in the databae.
As each table is created, multiple sub tables might be added 
because of nesting of child tags in the XML file. These additional]
tables are added to the reference table to keep track of the
structure of the XML file.
"""